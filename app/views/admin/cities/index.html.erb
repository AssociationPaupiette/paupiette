<% content_for :title, 'Villes' %>
<p><%= link_to 'Créer une ville', new_admin_city_path, class: 'btn btn-primary' %></p>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Nom</th>
      <th scope="col">Nombre d'utilisateurs</th>
    </tr>
  </thead>
  <tbody>
    <% @cities.each do |city| %>
      <tr>
        <td><%= city %></td>
        <td><%= city.users %></td>
      </tr>
    <% end %>
  </tbody>
</table>