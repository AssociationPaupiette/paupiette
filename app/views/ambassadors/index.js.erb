$(".users__list").append("<%= j(render(partial: 'ambassador', collection: @ambassadors, as: :ambassador)) %>");

<% if @ambassadors.last_page? %>
  $("#load-more-users").hide();
<% else %>
  $("#load-more-users").attr("href", "<%= ambassadors_path(page: @ambassadors.current_page+1) %>");
<% end %>