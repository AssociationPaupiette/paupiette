<nav class="navbar nav-marg navbar-light navbar-expand-md">
  <div class="container-fluid global-large offset-lg-1 col-lg-10 offset-lg-1 offset-md-1 col-md-10 offset-md-1 offset-sm-0 col-sm-12 offset-sm-0">
    <%= link_to image_tag('logo.png', width: 208), root_path, class: 'navbar-brand' %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item"><%= link_to t('menu.hosts'), hosts_path, class: 'nav-link nav-item-1' %></li>
        <li class="nav-item"><%= link_to t('menu.ambassadors'), ambassadors_path, class: 'nav-link nav-item-1' %></li>
        <% if current_user.nil? %>
          <li class="nav-item nav-item-3"><%= link_to t('menu.signin'), new_user_session_path, class: 'nav-link link-2' %> / <%= link_to t('menu.signup'), new_user_registration_path, class: 'nav-link link-2' %></li>
        <% else %>
          <li class="nav-item"><%= link_to t('my.name'), my_root_path, class: 'nav-link' %></li>

          <li class="nav-item dropdown">
            <a  id="navbarDropdownMenuUser" 
                class="nav-link dropdown-toggle" 
                href="#" 
                data-toggle="dropdown" 
                aria-haspopup="true" 
                aria-expanded="false">
              <%= current_user %>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuUser">
              <%= link_to t('my.messages.name'), my_messages_path, class: 'dropdown-item' %>
              <%= link_to t('my.profile.name'), my_profile_path, class: 'dropdown-item' %>
              <%= link_to t('my.reviews.name'), my_reviews_path, class: 'dropdown-item' %>
              <%= link_to t('my.meals.name'), my_meals_path, class: 'dropdown-item' %>
              <% if can? :manage, :city %>
                <%= link_to t('my_cities.name'), my_cities_root_path, class: 'dropdown-item' %>
              <% end %>
              <% if can? :access, :admin %>
                <%= link_to t('menu.admin'), admin_root_path, class: 'dropdown-item' %>
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to t('menu.signout'), destroy_user_session_path, class: 'dropdown-item' %>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>