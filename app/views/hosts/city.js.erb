$("#users").append("<%= j(render(partial: 'host', collection: @users, as: :host)) %>");

<% if @users.last_page? %>
  $("#load-more-users").hide();
<% else %>
  $("#load-more-users").attr("href", "<%= city_hosts_path(page: @users.current_page+1, city_slug: @city.slug ) %>");
<% end %>