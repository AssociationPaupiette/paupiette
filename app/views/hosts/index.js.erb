$("#users").append("<%= j(render(partial: 'users/user', collection: @users)) %>");

<% if @users.last_page? %>
  $("#load-more-users").hide();
<% else %>
  $("#load-more-users").attr("href", "<%= hosts_path(page: @users.current_page+1) %>");
<% end %>