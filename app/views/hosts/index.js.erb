$(".hosts-list").append("<%= j(render(partial: 'host', collection: @hosts, as: :host)) %>");

<% if @hosts.last_page? %>
  $("#load-more-hosts").hide();
<% else %>
  $("#load-more-hosts").attr("href", "<%= hosts_path(page: @hosts.current_page+1, city: @filter_city&.id) %>");
<% end %>